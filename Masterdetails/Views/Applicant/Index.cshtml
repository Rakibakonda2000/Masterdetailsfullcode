@model IEnumerable<Masterdetails.Models.Applicant>
@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TotalEx)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsAvaiable)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Picpath)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)

                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Date)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TotalEx)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IsAvaiable)
                </td>
                <td>
                    @if (item.Picpath != null)
                    {
                        <img src="@Url.Content(item.Picpath)" class="img-fluid" width="90" />
                    }
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
            <tr>
                <td colspan="5">
                    <table class="table table-bordered">
                        <thead>
                            <tr>
                                <th>Company</th>
                                <th>Designation</th>
                                <th>TotalEx</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var it in item.ApplicantExes)
                            {
                                <tr>
                                    <td>@it.CompanyName</td>
                                    <td>@it.Designation</td>
                                    <td>@it.YearofEx</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </td>
            </tr>
        }
    </tbody>
</table>

